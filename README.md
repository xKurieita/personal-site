#Create-react-xk-starter
A basic react template running via node and react-scripts.

To install run npx create-react-xk-starter ${name of repository}.

Solely made because creat-react-app was too much bloat.